const numbers: ReadonlyArray<number> = [1,2,2,2];
// numbers.push(9); no permitir mutar el array
// numbers.pop();
// numbers.unshift(1);
numbers.filter(()=> {})
numbers.reduce(() => 0)
numbers.map(() => 0)
